import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// const greeting = <p>Welcome to React!</p>;

// React component
// const Sum = (props) => {
//   return (
//     <p>
//       {props.num1} + {props.num2} = {props.num1 + props.num2}
//     </p>
//   );
// };

// const List = (props) => {
//   // go through array and display a li inside an ul for each element in the array
//   // ul ---> li li

//   return (
//     <ul>
//       {props.velsFavThings.map((thing) => {
//         return <li key={thing}>{thing}</li>;
//       })}
//     </ul>
//   );
// };
